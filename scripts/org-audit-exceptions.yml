# Findings the org audit knows about and is not going to fail on today.
#
# Every entry carries a reason and a destination. An exception with neither is
# how a permanent suppression starts, so both are required and the audit says
# so when it skips one.

archived:
  # Read-only on GitHub. Nobody can push a fix, and the code lives on inside
  # `kit` where the findings do not exist.
  - pagegen
  - slidegen
  - sheetgen
  - audiogen

scheduled: []
# Empty, and that is the goal state. Every entry that was here has been
# cleared by the retrofit it named: efl and fle adopted the shared caller
# at P3.4 and P3.3, taking their suppressions with it, and ressources'
# lychee steps moved to the weekly advisory job. The audit fails on an
# entry whose file no longer carries a suppression, so this list cannot
# quietly outlive its reasons.
